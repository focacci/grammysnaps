{
  "scripts": {
    "deploy:staging": "./scripts/deploy -e staging -b $(git branch --show-current)",
    "deploy:production": "./scripts/deploy -e production -b $(git branch --show-current)",
    "status": "./scripts/deployment-status"
  },
  "dependencies": {
    "canvas": "^3.1.2",
    "form-data": "^4.0.4",
    "node-fetch": "^2.7.0"
  }
}
